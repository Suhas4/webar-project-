# Local Netlify folder
.netlify

# Environment files (never commit secrets/keys)
.env
.env.local
.env.*.local

# Generated by the Netlify CLI when it bundles the edge function.
# Netlify regenerates it server-side on every deploy, so it is noise here.
deno.lock
