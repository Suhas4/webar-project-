<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local dev only: lets the debug build reach the Go backend running on the
         dev PC over plain HTTP. Scoped to the PC's LAN IP and loopback/emulator
         addresses only — does not weaken security for any real production host. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
