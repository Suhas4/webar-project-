# Node / Frontend
webar-app/node_modules/
webar-app/dist/
webar-app/.env
webar-app/.env.local
webar-app/.env.production
webar-app/deploy.zip

# Go / Backend
backend/webar-backend
backend/*.exe
backend/*.exe~
backend/.env
backend/backend_log.txt

# Logs
*.log
vite_log.txt

# Auto-provisioned local Postgres (used when DATABASE_URL isn't set)
memoera-embedded-postgres/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.suo
*.user

# Playwright / Claude artifacts
.playwright-mcp/
.claude/
write_components.py
certs/
